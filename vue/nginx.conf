server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Обработка маршрутов SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}